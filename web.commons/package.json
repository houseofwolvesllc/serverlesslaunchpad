{
  "name": "@houseofwolves/serverlesslaunchpad.web.commons",
  "version": "1.0.0",
  "description": "Shared framework-agnostic utilities, services, and logic for all web frontend projects",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@houseofwolves/serverlesslaunchpad.types": "*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.56.0",
    "typescript": "^5.2.2",
    "vitest": "^1.0.0"
  }
}
