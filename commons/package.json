{
    "name": "@houseofwolves/serverlesslaunchpad.commons",
    "version": "1.0.0",
    "author": "k-gar",
    "description": "Library for common types, schemas, and utils shared across the Serverless Launchpad platform.",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/src/index.d.ts",
    "type": "module",
    "scripts": {
        "build": "tsc && fix-esm-import-path dist",
        "typecheck": "tsc --noEmit",
        "test": "vitest --coverage",
        "test:watch": "vitest --watch"
    },
    "devDependencies": {
        "fix-esm-import-path": "^1.10.3",
        "vitest": "^2.1.8"
    },
    "dependencies": {
        "zod": "^3.24.2"
    }
}
