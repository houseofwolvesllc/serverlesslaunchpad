/**
 * Link Navigator - Re-export from web.commons
 *
 * The LinkNavigator is pure TypeScript with no framework dependencies,
 * so we can re-export it directly from web.commons.
 */

export {
    LinkNavigator,
    createLinkNavigator,
    linkNavigator, // Singleton instance
} from '@houseofwolves/serverlesslaunchpad.web.commons';
