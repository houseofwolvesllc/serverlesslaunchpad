/**
 * Web Configuration Schema - Re-export from web.commons
 *
 * The configuration schema is pure TypeScript with Zod, so we can
 * re-export it directly from web.commons.
 */

export { WebConfigSchema, type WebConfig } from '@houseofwolves/serverlesslaunchpad.web.commons';
